# 🤝 Участие в разработке

Руководство по участию в разработке проекта.

---

## Workflow

### 1. Форк репозитория

Перейдите на страницу репозитория и нажмите кнопку "Fork" для создания вашей копии.

### 2. Клонирование форка

```bash
git clone git@github.com:YOUR_USERNAME/mini-crm-ai-gen.git
cd mini-crm-ai-gen
```

### 3. Настройка upstream

Добавьте оригинальный репозиторий как upstream:

```bash
git remote add upstream git@github.com:bgs2509/mini-crm-ai-gen.git
git fetch upstream
```

### 4. Создание ветки для новой функции

Всегда создавайте новую ветку для вашей работы:

```bash
# Переключение на главную ветку
git checkout master

# Обновление из upstream
git pull upstream master

# Создание новой ветки
git checkout -b feature/amazing-feature
```

**Соглашения по именованию веток:**
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов
- `chore/` - обновление зависимостей, конфигурации

**Примеры:**
```bash
git checkout -b feature/add-email-notifications
git checkout -b fix/contact-deletion-bug
git checkout -b docs/update-api-documentation
```

### 5. Разработка

Внесите необходимые изменения:

- Пишите чистый, читаемый код
- Следуйте существующему стилю кода
- Добавляйте комментарии где необходимо
- Пишите тесты для новой функциональности
- Обновляйте документацию при необходимости

### 6. Запуск проверок качества

Перед коммитом запустите все проверки:

```bash
# Форматирование кода
black .

# Сортировка импортов
isort .

# Линтинг
ruff check .

# Проверка типов
mypy app/

# Запуск тестов
pytest

# С покрытием кода
pytest --cov=app --cov-report=html
```

**Или используйте pre-commit хуки:**

```bash
# Установка pre-commit
pre-commit install

# Автоматический запуск при коммите
git commit -m "your message"
```

### 7. Коммит изменений

Используйте [Conventional Commits](https://www.conventionalcommits.org/) для сообщений коммитов:

```bash
git add .
git commit -m "feat: Add amazing feature"
```

**Формат коммитов:**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы коммитов:**
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода (не влияет на логику)
- `refactor:` - рефакторинг кода
- `test:` - добавление или изменение тестов
- `chore:` - обновление зависимостей, конфигурации, задач сборки

**Примеры:**
```bash
git commit -m "feat(contacts): Add search by phone number"
git commit -m "fix(deals): Prevent deletion of deals with active tasks"
git commit -m "docs(api): Update authentication examples"
git commit -m "test(services): Add unit tests for deal service"
```

### 8. Отправка в удаленную ветку

```bash
git push origin feature/amazing-feature
```

### 9. Создание Pull Request

1. Перейдите на страницу репозитория
2. Нажмите "New Pull Request"
3. Выберите вашу ветку
4. Заполните шаблон Pull Request (см. ниже)
5. Нажмите "Create Pull Request"

---

## Стиль кода

### Python

Проект использует следующие инструменты для обеспечения единого стиля:

- **black** - автоматическое форматирование
- **ruff** - линтер (замена flake8, pylint)
- **isort** - сортировка импортов
- **mypy** - проверка типов

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#качество-кода-и-линтеры)

### Основные правила

1. **Type hints обязательны:**
   ```python
   def get_contact(contact_id: UUID, organization_id: UUID) -> Contact:
       ...
   ```

2. **Docstrings для публичных функций:**
   ```python
   def create_deal(
       deal_data: DealCreate,
       organization_id: UUID,
       user_id: UUID
   ) -> Deal:
       """
       Создать новую сделку.

       Args:
           deal_data: Данные для создания сделки
           organization_id: ID организации
           user_id: ID пользователя (владелец)

       Returns:
           Созданная сделка

       Raises:
           ValueError: Если сумма сделки отрицательная
       """
   ```

3. **Использование async/await:**
   ```python
   async def list_contacts(
       organization_id: UUID,
       skip: int = 0,
       limit: int = 10
   ) -> list[Contact]:
       ...
   ```

4. **Именование:**
   - Функции и переменные: `snake_case`
   - Классы: `PascalCase`
   - Константы: `UPPER_SNAKE_CASE`
   - Приватные методы: начинаются с `_`

### Импорты

Используйте `isort` для автоматической сортировки импортов:

```python
# Стандартная библиотека
from typing import Optional
from uuid import UUID

# Сторонние библиотеки
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

# Локальные импорты
from app.models.contact import Contact
from app.schemas.contact import ContactCreate, ContactResponse
```

---

## Тестирование

### Структура тестов

```
tests/
├── unit/              # Unit тесты
│   ├── test_services/
│   └── test_repositories/
├── integration/       # Интеграционные тесты
│   └── test_api/
└── e2e/              # End-to-end тесты
    └── test_full_flow.py
```

### Написание тестов

**Unit тесты** - тестируют изолированную функциональность:
```python
async def test_create_contact_success():
    # Arrange
    contact_data = ContactCreate(name="Test", email="test@example.com")

    # Act
    result = await contact_service.create(contact_data, org_id, user_id)

    # Assert
    assert result.name == "Test"
    assert result.email == "test@example.com"
```

**Интеграционные тесты** - тестируют взаимодействие компонентов:
```python
async def test_create_contact_api(client: AsyncClient):
    response = await client.post(
        "/api/v1/contacts",
        json={"name": "Test", "email": "test@example.com"},
        headers={"X-Organization-Id": str(org_id)}
    )
    assert response.status_code == 201
```

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#тестирование)

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Конкретный файл
pytest tests/unit/test_services/test_contact_service.py

# Конкретный тест
pytest tests/unit/test_services/test_contact_service.py::test_create_contact
```

---

## Code Review

### Pull Request должен:

1. **Проходить все тесты:**
   - Все существующие тесты проходят
   - Новые тесты добавлены для новой функциональности
   - Покрытие кода не снижается

2. **Иметь покрытие тестами:**
   - Новая функциональность покрыта тестами
   - Unit тесты для бизнес-логики
   - Интеграционные тесты для API эндпоинтов

3. **Соответствовать стилю кода:**
   - Код отформатирован с помощью `black`
   - Импорты отсортированы через `isort`
   - Линтер `ruff` не выдает ошибок

4. **Иметь type hints:**
   - Все функции имеют аннотации типов
   - `mypy` не выдает ошибок
   - Типы соответствуют реальным значениям

5. **Содержать описание изменений:**
   - Четкое описание того, что изменено
   - Объяснение "почему" (если не очевидно)
   - Примеры использования (если нужно)
   - Скриншоты (если UI изменения)

6. **Обновлять документацию:**
   - API документация обновлена (если API изменен)
   - README обновлен (если необходимо)
   - Примеры обновлены (если API изменился)

### Процесс Review

1. **Автоматические проверки:**
   - CI/CD запускает тесты и проверки
   - Все проверки должны пройти успешно

2. **Ручной review:**
   - Минимум 1 одобрение от maintainer
   - Комментарии должны быть адресованы
   - Исправления должны быть внесены

3. **После одобрения:**
   - PR будет слит в master ветку
   - Автоматическое закрытие связанных issues

---

## Шаблон Pull Request

```markdown
## Описание

Краткое описание изменений.

## Тип изменений

- [ ] Новая функциональность (не ломающая существующий код)
- [ ] Исправление бага (не ломающее существующий код)
- [ ] Изменение, ломающее существующий код (требует миграции)
- [ ] Изменения в документации

## Связанные Issues

Закрывает #(номер issue)

## Изменения

- Что изменено
- Что добавлено
- Что удалено

## Тестирование

Как тестировались изменения:
- [ ] Unit тесты добавлены/обновлены
- [ ] Интеграционные тесты добавлены/обновлены
- [ ] Локальное тестирование выполнено

## Чеклист

- [ ] Код следует стилю проекта (black, ruff, isort)
- [ ] Type hints добавлены (mypy проходит)
- [ ] Тесты добавлены и все проходят
- [ ] Документация обновлена
- [ ] Коммиты следуют Conventional Commits

## Скриншоты (если применимо)

[Добавьте скриншоты, если изменения видны в UI]

## Дополнительная информация

Любая дополнительная информация для review.
```

---

## Разрешение конфликтов

### Обновление вашей ветки

Если ваша ветка отстала от master:

```bash
# Переключение на вашу ветку
git checkout feature/amazing-feature

# Получение последних изменений из upstream
git fetch upstream

# Слияние master в вашу ветку
git merge upstream/master

# Или использование rebase (предпочтительнее)
git rebase upstream/master
```

### Решение конфликтов

1. Git укажет файлы с конфликтами
2. Откройте файлы и найдите маркеры конфликтов:
   ```
   <<<<<<< HEAD
   ваш код
   =======
   код из master
   >>>>>>> upstream/master
   ```
3. Разрешите конфликты вручную
4. Удалите маркеры конфликтов
5. Добавьте файлы:
   ```bash
   git add <resolved-files>
   ```
6. Продолжите слияние:
   ```bash
   git commit  # для merge
   # или
   git rebase --continue  # для rebase
   ```

---

## Получение помощи

Если у вас возникли вопросы:

1. **Проверьте документацию:**
   - [README.md](../README.md)
   - [DEVELOPMENT.md](DEVELOPMENT.md)
   - [API.md](API.md)

2. **Создайте Issue:**
   - Вопросы по использованию
   - Сообщения о багах
   - Предложения по улучшению

3. **Свяжитесь с maintainer:**
   - Через Issues в репозитории
   - В комментариях к Pull Request

---

## Признательность

Спасибо за ваш интерес к проекту! Каждый вклад важен и ценится.

---

**Подробнее:**
- [DEVELOPMENT.md](DEVELOPMENT.md) - Детальная документация для разработчиков
- [API.md](API.md) - Документация API
- [Conventional Commits](https://www.conventionalcommits.org/) - Стандарт коммитов

**Назад к:** [README.md](../README.md)
