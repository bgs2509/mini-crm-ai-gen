# 📚 API Документация

Полная документация по всем эндпоинтам API v1.

## Аутентификация

### POST `/api/v1/auth/register`

Регистрация нового пользователя и создание организации.

**Запрос:**
```json
{
  "email": "user@example.com",
  "password": "SecurePass123",
  "name": "Иван Иванов",
  "organization_name": "Моя компания"
}
```

**Ответ:**
```json
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "Иван Иванов"
  },
  "organization": {
    "id": "uuid",
    "name": "Моя компания"
  }
}
```

### POST `/api/v1/auth/login`

Вход в систему и получение JWT токенов.

**Запрос:**
```json
{
  "email": "user@example.com",
  "password": "SecurePass123"
}
```

**Ответ:**
```json
{
  "access_token": "eyJ...",
  "refresh_token": "eyJ...",
  "token_type": "bearer"
}
```

### POST `/api/v1/auth/refresh`

Обновление access токена с помощью refresh токена.

**Запрос:**
```json
{
  "refresh_token": "eyJ..."
}
```

**Ответ:**
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer"
}
```

---

## Организации

### GET `/api/v1/organizations/me`

Получить список организаций текущего пользователя.

**Заголовки:**
- `Authorization: Bearer <access_token>`

**Ответ:**
```json
[
  {
    "id": "uuid",
    "name": "Моя компания",
    "created_at": "2025-01-01T00:00:00Z"
  }
]
```

---

## Контакты

**Базовый URL:** `/api/v1/contacts`

**Заголовки (для всех эндпоинтов):**
- `Authorization: Bearer <access_token>`
- `X-Organization-Id: <organization_id>`

### GET `/api/v1/contacts`

Список контактов с поиском и пагинацией.

**Параметры запроса:**
- `page` (int, default=1) - номер страницы
- `page_size` (int, max=100) - размер страницы
- `search` (string, optional) - поиск по имени/email
- `owner_id` (uuid, optional) - фильтр по владельцу (для manager+)

**Ответ:**
```json
{
  "items": [
    {
      "id": "uuid",
      "name": "Алексей Петров",
      "email": "alexey@example.com",
      "phone": "+79161234567",
      "created_at": "2025-01-01T00:00:00Z"
    }
  ],
  "total": 100,
  "page": 1,
  "page_size": 20
}
```

### POST `/api/v1/contacts`

Создать новый контакт.

**Запрос:**
```json
{
  "name": "Алексей Петров",
  "email": "alexey@example.com",
  "phone": "+79161234567"
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "name": "Алексей Петров",
  "email": "alexey@example.com",
  "phone": "+79161234567",
  "created_at": "2025-01-01T00:00:00Z"
}
```

### GET `/api/v1/contacts/{id}`

Получить детали контакта.

**Ответ:**
```json
{
  "id": "uuid",
  "name": "Алексей Петров",
  "email": "alexey@example.com",
  "phone": "+79161234567",
  "created_at": "2025-01-01T00:00:00Z"
}
```

### PUT `/api/v1/contacts/{id}`

Обновить контакт.

**Запрос:**
```json
{
  "name": "Алексей Петров",
  "email": "new@example.com",
  "phone": "+79167654321"
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "name": "Алексей Петров",
  "email": "new@example.com",
  "phone": "+79167654321",
  "created_at": "2025-01-01T00:00:00Z"
}
```

### DELETE `/api/v1/contacts/{id}`

Удалить контакт (только если нет связанных сделок).

**Ответ:** `204 No Content`

**Ошибка (409 Conflict):**
```json
{
  "detail": "Невозможно удалить контакт: есть активные сделки",
  "code": "CONFLICT"
}
```

---

## Сделки

**Базовый URL:** `/api/v1/deals`

**Заголовки (для всех эндпоинтов):**
- `Authorization: Bearer <access_token>`
- `X-Organization-Id: <organization_id>`

### GET `/api/v1/deals`

Список сделок с фильтрацией и пагинацией.

**Параметры запроса:**
- `page` (int, default=1) - номер страницы
- `page_size` (int, max=100) - размер страницы
- `status` (array, optional) - фильтр по статусам: `status=new&status=in_progress`
- `min_amount` (float, optional) - минимальная сумма
- `max_amount` (float, optional) - максимальная сумма
- `stage` (string, optional) - стадия воронки
- `owner_id` (uuid, optional) - фильтр по владельцу (для ролей выше member)
- `order_by` (string, optional) - поле сортировки: `created_at`, `amount`
- `order` (string, optional) - направление сортировки: `asc`, `desc`

**Ответ:**
```json
{
  "items": [
    {
      "id": "uuid",
      "title": "Редизайн сайта",
      "amount": 150000.0,
      "currency": "RUB",
      "status": "in_progress",
      "stage": "proposal",
      "contact_id": "uuid",
      "owner_id": "uuid",
      "created_at": "2025-01-01T00:00:00Z",
      "updated_at": "2025-01-02T00:00:00Z"
    }
  ],
  "total": 50,
  "page": 1,
  "page_size": 20
}
```

### POST `/api/v1/deals`

Создать новую сделку.

**Запрос:**
```json
{
  "contact_id": "uuid",
  "title": "Редизайн сайта",
  "amount": 150000.0,
  "currency": "RUB",
  "stage": "qualification"
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "title": "Редизайн сайта",
  "amount": 150000.0,
  "currency": "RUB",
  "status": "new",
  "stage": "qualification",
  "contact_id": "uuid",
  "owner_id": "uuid",
  "created_at": "2025-01-01T00:00:00Z",
  "updated_at": "2025-01-01T00:00:00Z"
}
```

### GET `/api/v1/deals/{id}`

Получить детали сделки.

**Ответ:**
```json
{
  "id": "uuid",
  "title": "Редизайн сайта",
  "amount": 150000.0,
  "currency": "RUB",
  "status": "in_progress",
  "stage": "proposal",
  "contact_id": "uuid",
  "owner_id": "uuid",
  "created_at": "2025-01-01T00:00:00Z",
  "updated_at": "2025-01-02T00:00:00Z"
}
```

### PATCH `/api/v1/deals/{id}`

Частичное обновление сделки.

**Запрос:**
```json
{
  "status": "won",
  "stage": "closed",
  "amount": 180000.0
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "title": "Редизайн сайта",
  "amount": 180000.0,
  "currency": "RUB",
  "status": "won",
  "stage": "closed",
  "contact_id": "uuid",
  "owner_id": "uuid",
  "created_at": "2025-01-01T00:00:00Z",
  "updated_at": "2025-01-03T00:00:00Z"
}
```

**Бизнес-правила:**
- При `status="won"` → `amount > 0`, иначе возвращается `400 Bad Request`
- Откат стадии назад запрещен для роли `member`
- При смене статуса автоматически создается запись в `activities`

### DELETE `/api/v1/deals/{id}`

Удалить сделку.

**Ответ:** `204 No Content`

---

## Задачи

**Базовый URL:** `/api/v1/tasks`

**Заголовки (для всех эндпоинтов):**
- `Authorization: Bearer <access_token>`
- `X-Organization-Id: <organization_id>`

### GET `/api/v1/tasks`

Список задач с фильтрацией.

**Параметры запроса:**
- `deal_id` (uuid, optional) - фильтр по сделке
- `only_open` (bool, optional) - только невыполненные задачи
- `due_before` (date, optional) - задачи с дедлайном до даты
- `due_after` (date, optional) - задачи с дедлайном после даты

**Ответ:**
```json
{
  "items": [
    {
      "id": "uuid",
      "deal_id": "uuid",
      "title": "Позвонить клиенту",
      "description": "Обсудить детали предложения",
      "due_date": "2025-01-15",
      "is_done": false,
      "created_at": "2025-01-01T00:00:00Z"
    }
  ],
  "total": 25
}
```

### POST `/api/v1/tasks`

Создать задачу.

**Запрос:**
```json
{
  "deal_id": "uuid",
  "title": "Позвонить клиенту",
  "description": "Обсудить детали предложения",
  "due_date": "2025-01-15"
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "deal_id": "uuid",
  "title": "Позвонить клиенту",
  "description": "Обсудить детали предложения",
  "due_date": "2025-01-15",
  "is_done": false,
  "created_at": "2025-01-01T00:00:00Z"
}
```

**Валидация:**
- `due_date` не может быть в прошлом
- `deal_id` должен принадлежать текущей организации
- Для роли `member` - сделка должна принадлежать пользователю

### GET `/api/v1/tasks/{id}`

Получить детали задачи.

**Ответ:**
```json
{
  "id": "uuid",
  "deal_id": "uuid",
  "title": "Позвонить клиенту",
  "description": "Обсудить детали предложения",
  "due_date": "2025-01-15",
  "is_done": false,
  "created_at": "2025-01-01T00:00:00Z"
}
```

### PATCH `/api/v1/tasks/{id}`

Обновить задачу (например, пометить как выполненную).

**Запрос:**
```json
{
  "is_done": true
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "deal_id": "uuid",
  "title": "Позвонить клиенту",
  "description": "Обсудить детали предложения",
  "due_date": "2025-01-15",
  "is_done": true,
  "created_at": "2025-01-01T00:00:00Z"
}
```

### DELETE `/api/v1/tasks/{id}`

Удалить задачу.

**Ответ:** `204 No Content`

---

## Активности (таймлайн)

**Базовый URL:** `/api/v1/deals/{deal_id}/activities`

**Заголовки (для всех эндпоинтов):**
- `Authorization: Bearer <access_token>`
- `X-Organization-Id: <organization_id>`

### GET `/api/v1/deals/{deal_id}/activities`

Получить таймлайн активности по сделке.

**Ответ:**
```json
{
  "items": [
    {
      "id": "uuid",
      "deal_id": "uuid",
      "author_id": "uuid",
      "type": "comment",
      "payload": {
        "text": "Клиент попросил обновленное предложение"
      },
      "created_at": "2025-01-01T12:00:00Z"
    },
    {
      "id": "uuid",
      "deal_id": "uuid",
      "author_id": null,
      "type": "status_changed",
      "payload": {
        "old_status": "new",
        "new_status": "in_progress"
      },
      "created_at": "2025-01-01T10:00:00Z"
    }
  ],
  "total": 10
}
```

**Типы активностей:**
- `comment` - комментарий пользователя
- `status_changed` - изменение статуса сделки
- `task_created` - создание задачи
- `system` - системные события

### POST `/api/v1/deals/{deal_id}/activities`

Создать комментарий (только `type="comment"`).

**Запрос:**
```json
{
  "type": "comment",
  "payload": {
    "text": "Клиент попросил обновленное предложение"
  }
}
```

**Ответ:**
```json
{
  "id": "uuid",
  "deal_id": "uuid",
  "author_id": "uuid",
  "type": "comment",
  "payload": {
    "text": "Клиент попросил обновленное предложение"
  },
  "created_at": "2025-01-01T12:00:00Z"
}
```

**Примечание:** Системные активности (изменение статуса, создание задач) создаются автоматически.

---

## Аналитика

**Заголовки (для всех эндпоинтов):**
- `Authorization: Bearer <access_token>`
- `X-Organization-Id: <organization_id>`

### GET `/api/v1/analytics/deals/summary`

Сводка по сделкам текущей организации.

**Ответ:**
```json
{
  "by_status": {
    "new": {
      "count": 15,
      "total_amount": 500000.0
    },
    "in_progress": {
      "count": 8,
      "total_amount": 350000.0
    },
    "won": {
      "count": 23,
      "total_amount": 1200000.0
    },
    "lost": {
      "count": 5,
      "total_amount": 150000.0
    }
  },
  "average_won_amount": 52173.91,
  "new_deals_last_30_days": 12
}
```

### GET `/api/v1/analytics/deals/funnel`

Воронка продаж с конверсией.

**Ответ:**
```json
{
  "funnel": [
    {
      "stage": "qualification",
      "count": 25,
      "by_status": {
        "new": 15,
        "in_progress": 8,
        "won": 2
      },
      "conversion_rate": 100.0
    },
    {
      "stage": "proposal",
      "count": 18,
      "by_status": {
        "in_progress": 12,
        "won": 5,
        "lost": 1
      },
      "conversion_rate": 72.0
    },
    {
      "stage": "negotiation",
      "count": 12,
      "by_status": {
        "in_progress": 8,
        "won": 4
      },
      "conversion_rate": 66.67
    },
    {
      "stage": "closed",
      "count": 8,
      "by_status": {
        "won": 7,
        "lost": 1
      },
      "conversion_rate": 66.67
    }
  ]
}
```

---

## Формат ошибок

Все ошибки возвращаются в едином формате:

```json
{
  "detail": "Описание ошибки",
  "code": "ERROR_CODE"
}
```

### Коды ошибок

- `400 Bad Request` - Ошибка валидации (ValidationError)
- `401 Unauthorized` - Не авторизован (Unauthorized)
- `403 Forbidden` - Нет прав доступа (Forbidden)
- `404 Not Found` - Ресурс не найден
- `409 Conflict` - Конфликт (например, нельзя удалить контакт с активными сделками)
- `422 Unprocessable Entity` - Ошибка обработки данных

### Примеры ошибок

**400 Bad Request:**
```json
{
  "detail": "Невозможно закрыть сделку как выигранную с нулевой суммой",
  "code": "VALIDATION_ERROR"
}
```

**401 Unauthorized:**
```json
{
  "detail": "Неверные учетные данные",
  "code": "UNAUTHORIZED"
}
```

**403 Forbidden:**
```json
{
  "detail": "Недостаточно прав для выполнения операции",
  "code": "FORBIDDEN"
}
```

**404 Not Found:**
```json
{
  "detail": "Контакт не найден",
  "code": "NOT_FOUND"
}
```

**409 Conflict:**
```json
{
  "detail": "Невозможно удалить контакт: есть активные сделки",
  "code": "CONFLICT"
}
```

---

## Аутентификация

Все защищенные эндпоинты требуют:

1. **Access Token** в заголовке:
   ```
   Authorization: Bearer <access_token>
   ```

2. **Organization ID** в заголовке (для всех операций кроме аутентификации):
   ```
   X-Organization-Id: <organization_id>
   ```

Access token действителен 15 минут. Для обновления используйте `/api/v1/auth/refresh` с refresh token (действителен 7 дней).

---

## Пагинация

Все списки поддерживают пагинацию через параметры `page` и `page_size`:

- `page` - номер страницы (начиная с 1)
- `page_size` - количество элементов на странице (максимум 100)

Ответ содержит:
```json
{
  "items": [...],
  "total": 150,
  "page": 1,
  "page_size": 20
}
```

---

## Версионирование

Текущая версия API: **v1**

Все эндпоинты доступны по пути `/api/v1/...`

В будущем планируется поддержка версий API через заголовок `Accept` или в URL.

