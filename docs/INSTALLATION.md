# 📦 Установка и настройка

Подробные инструкции по установке и настройке CRM системы.

---

## Системные требования

### Минимальные требования

- **Python:** 3.11 или выше
- **PostgreSQL:** 15 или выше
- **Docker & Docker Compose:** последние версии (для Docker-установки)
- **RAM:** минимум 2 GB (рекомендуется 4 GB)
- **Диск:** минимум 5 GB свободного места

### Рекомендуемые требования

- **Python:** 3.12
- **PostgreSQL:** 16
- **RAM:** 4 GB или больше
- **Диск:** 10 GB или больше (для логов и бэкапов)

---

## Вариант 1: Запуск через Docker (рекомендуется)

Самый простой способ запустить систему - использовать Docker Compose.

### Шаг 1: Клонирование репозитория

```bash
git clone git@github.com:bgs2509/mini-crm-ai-gen.git
cd mini-crm-ai-gen
```

### Шаг 2: Копирование файла окружения

```bash
cp .env.example .env
```

### Шаг 3: Настройка переменных окружения

Откройте файл `.env` и настройте необходимые параметры:

```env
# Секретный ключ для JWT (минимум 32 символа)
SECRET_KEY=your-secure-secret-key-minimum-32-characters-long

# Пароль для PostgreSQL
POSTGRES_PASSWORD=your-secure-password

# Настройки базы данных
POSTGRES_DB=minicrm
POSTGRES_USER=minicrm_user

# Настройки приложения
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# Настройки CORS (разделять запятой)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Настройки JWT
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
```

> ⚠️ **Важно:** В production обязательно измените `SECRET_KEY` на случайную строку длиной не менее 32 символов.

### Шаг 4: Запуск приложения

```bash
docker-compose up -d
```

Эта команда:
- Создаст и запустит контейнер PostgreSQL
- Создаст и запустит контейнер приложения
- Автоматически применит миграции базы данных
- Запустит API сервер на порту 8000

### Шаг 5: Проверка работы

```bash
# Проверка статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs -f app

# Проверка здоровья API
curl http://localhost:8000/health
```

### Шаг 6: Доступ к приложению

| Сервис | URL | Описание |
|--------|-----|----------|
| 🌐 API | http://localhost:8000 | Основной API эндпоинт |
| 📚 Swagger UI | http://localhost:8000/docs | Интерактивная документация API |
| 📖 ReDoc | http://localhost:8000/redoc | Альтернативная документация API |
| 💚 Health Check | http://localhost:8000/health | Проверка здоровья системы |
| 🗄️ PostgreSQL | localhost:5432 | База данных (только внутри Docker сети) |

### Остановка и удаление

```bash
# Остановка контейнеров
docker-compose down

# Остановка с удалением volumes (удалит данные БД!)
docker-compose down -v
```

---

## Вариант 2: Установка без Docker

Если вы предпочитаете запускать приложение без Docker.

### Шаг 1: Установка PostgreSQL

#### Ubuntu/Debian

```bash
sudo apt update
sudo apt install postgresql-15 postgresql-contrib-15
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

#### macOS (Homebrew)

```bash
brew install postgresql@15
brew services start postgresql@15
```

#### Windows

Скачайте установщик с официального сайта: https://www.postgresql.org/download/windows/

### Шаг 2: Создание базы данных

```bash
# Подключение к PostgreSQL
sudo -u postgres psql

# Создание базы данных и пользователя
CREATE DATABASE minicrm;
CREATE USER minicrm_user WITH PASSWORD 'your-secure-password';
GRANT ALL PRIVILEGES ON DATABASE minicrm TO minicrm_user;
\q
```

### Шаг 3: Клонирование и настройка Python окружения

```bash
# Клонирование репозитория
git clone git@github.com:bgs2509/mini-crm-ai-gen.git
cd mini-crm-ai-gen

# Создание виртуального окружения
python3.11 -m venv venv

# Активация виртуального окружения
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=your-secure-secret-key-minimum-32-characters
DATABASE_URL=postgresql+asyncpg://minicrm_user:your-secure-password@localhost:5432/minicrm
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
```

### Шаг 5: Применение миграций

```bash
# Применение всех миграций
alembic upgrade head
```

### Шаг 6: Запуск приложения

```bash
# Запуск в режиме разработки
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Или запуск через gunicorn для production
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

---

## Устранение неполадок

### Проблема: Контейнер не запускается

**Симптомы:**
```bash
docker-compose ps
# Контейнер показывает статус "Exit 1" или "Restarting"
```

**Решение:**
```bash
# Просмотр логов для диагностики
docker-compose logs app

# Проверка конфигурации
docker-compose config

# Пересборка образов
docker-compose up --build
```

### Проблема: Ошибка подключения к базе данных

**Симптомы:**
```
sqlalchemy.exc.OperationalError: could not connect to server
```

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```bash
   docker-compose ps postgres
   ```
2. Проверьте переменные окружения в `.env`
3. Проверьте, что миграции применены:
   ```bash
   docker-compose exec app alembic current
   ```

### Проблема: Порт 8000 уже занят

**Симптомы:**
```
Error: bind: address already in use
```

**Решение:**
1. Измените порт в `docker-compose.yml`:
   ```yaml
   ports:
     - "8001:8000"  # Использовать порт 8001 вместо 8000
   ```
2. Или остановите процесс, занимающий порт:
   ```bash
   # Linux/macOS
   lsof -ti:8000 | xargs kill -9
   ```

### Проблема: Миграции не применяются

**Симптомы:**
```
alembic.util.exc.CommandError: Can't locate revision identified by 'xxx'
```

**Решение:**
```bash
# Сброс миграций (осторожно - удалит данные!)
docker-compose down -v
docker-compose up -d

# Или вручную применить миграции
docker-compose exec app alembic upgrade head
```

### Проблема: Ошибки валидации Pydantic

**Симптомы:**
```
pydantic.ValidationError: field required
```

**Решение:**
1. Проверьте все обязательные переменные окружения в `.env`
2. Убедитесь, что `SECRET_KEY` имеет минимум 32 символа
3. Проверьте формат `DATABASE_URL` или `POSTGRES_*` переменных

---

## Требования к системным ресурсам

### Для разработки

- **CPU:** 2 ядра
- **RAM:** 2 GB
- **Диск:** 5 GB
- **Сеть:** стабильное подключение к интернету (для загрузки Docker образов)

### Для production

- **CPU:** 4+ ядра
- **RAM:** 4+ GB (8 GB рекомендуется)
- **Диск:** 20+ GB (SSD рекомендуется)
- **Сеть:** стабильное подключение с низкой задержкой

### Рекомендации для больших нагрузок

- **База данных:** выделенный сервер PostgreSQL
- **Приложение:** несколько экземпляров за load balancer
- **Кеширование:** Redis для кеширования аналитики
- **Мониторинг:** Prometheus + Grafana

---

## Проверка установки

После установки выполните следующие проверки:

### 1. Проверка здоровья API

```bash
curl http://localhost:8000/health
```

Ожидаемый ответ:
```json
{
  "status": "healthy",
  "environment": "development",
  "version": "1.0.0"
}
```

### 2. Проверка документации

Откройте в браузере:
- http://localhost:8000/docs (Swagger UI)
- http://localhost:8000/redoc (ReDoc)

### 3. Проверка базы данных

```bash
# Через Docker
docker-compose exec postgres psql -U minicrm_user -d minicrm -c "\dt"

# Или напрямую (если установлен локально)
psql -U minicrm_user -d minicrm -c "\dt"
```

Должны быть созданы таблицы: `users`, `organizations`, `contacts`, `deals`, `tasks`, `activities`, и т.д.

### 4. Тестовый запрос API

```bash
# Регистрация тестового пользователя
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPass123",
    "name": "Test User",
    "organization_name": "Test Org"
  }'
```

Если запрос успешен, установка завершена правильно.

---

## Следующие шаги

После успешной установки:

1. Ознакомьтесь с [API документацией](API.md)
2. Изучите [примеры использования](EXAMPLES.md)
3. Прочитайте [руководство по разработке](DEVELOPMENT.md)
4. Проверьте [требования безопасности](SECURITY.md)

---

**Назад к:** [README.md](../README.md)
