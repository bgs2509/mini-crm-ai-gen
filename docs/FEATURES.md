# ✨ Возможности системы

Подробное описание всех функций и возможностей CRM системы.

---

## 🔐 Аутентификация и авторизация

### JWT аутентификация

Система использует JSON Web Tokens (JWT) для безопасной аутентификации пользователей:

- **Access Token** - короткоживущий токен (15 минут) для доступа к API
- **Refresh Token** - долгоживущий токен (7 дней) для обновления access токена
- **Bearer Authentication** - токены передаются в заголовке `Authorization: Bearer <token>`

**Эндпоинты:**
- `POST /api/v1/auth/register` - Регистрация нового пользователя
- `POST /api/v1/auth/login` - Вход в систему
- `POST /api/v1/auth/refresh` - Обновление access токена

**Подробнее:** [API.md](API.md#аутентификация)

### Хеширование паролей

Все пароли хранятся в хешированном виде с использованием **bcrypt**:

- Автоматическое хеширование при регистрации
- Безопасное сравнение паролей при входе
- Пароли никогда не хранятся в открытом виде
- Соль генерируется автоматически для каждого пароля

### Role-Based Access Control (RBAC)

Система поддерживает 4 уровня ролей с гранулярным контролем доступа:

#### 1. **Owner** (Владелец)
- Полный доступ ко всем функциям организации
- Управление участниками организации
- Изменение настроек организации
- Доступ ко всем контактам, сделкам и задачам

#### 2. **Admin** (Администратор)
- Управление организацией и участниками
- Полный доступ ко всем сущностям (контакты, сделки, задачи)
- Не может удалить организацию или владельца
- Может назначать роли другим пользователям

#### 3. **Manager** (Менеджер)
- Просмотр всех контактов, сделок и задач
- Создание и редактирование любых сущностей
- Управление сделками в любой стадии
- Не может управлять участниками организации

#### 4. **Member** (Участник)
- Просмотр всех контактов, сделок и задач
- Создание и редактирование только своих контактов
- Создание задач только для своих сделок
- Ограниченные возможности по управлению сделками

**Подробнее:** [API.md](API.md#организации)

### Обработка HTTP ошибок

Система возвращает корректные HTTP коды состояния:

- **400 Bad Request** - ошибки валидации входных данных
- **401 Unauthorized** - отсутствие или недействительный токен
- **403 Forbidden** - недостаточно прав доступа
- **404 Not Found** - ресурс не найден
- **409 Conflict** - конфликт бизнес-логики (например, попытка удалить контакт с активными сделками)
- **422 Unprocessable Entity** - ошибки валидации Pydantic

---

## 🏢 Multi-tenancy (Многопользовательская аренда)

### Изоляция данных

Каждая организация имеет полную изоляцию данных:

- Все сущности (контакты, сделки, задачи) привязаны к организации
- Данные одной организации недоступны другим организациям
- Автоматическая проверка принадлежности ресурсов к организации
- Защита от межорганизационного доступа (возвращает 403/404)

### Множественные организации

Пользователь может принадлежать к нескольким организациям:

- Один пользователь может быть участником разных организаций
- Для каждого доступа нужно указывать контекст организации
- Разные роли в разных организациях
- Легкое переключение между организациями

### Контекст организации

Контекст организации задается через HTTP заголовок:

```
X-Organization-Id: <organization_id>
```

- Заголовок обязателен для всех операций с данными
- Система автоматически фильтрует данные по организации
- Проверка принадлежности пользователя к организации
- Валидация существования организации

**Подробнее:** [API.md](API.md#организации)

---

## 📇 Управление контактами

### CRUD операции

Полный набор операций для работы с контактами:

- **GET /api/v1/contacts** - список контактов с пагинацией
- **POST /api/v1/contacts** - создание нового контакта
- **GET /api/v1/contacts/{id}** - получение контакта по ID
- **PATCH /api/v1/contacts/{id}** - обновление контакта
- **DELETE /api/v1/contacts/{id}** - удаление контакта

**Подробнее:** [API.md](API.md#контакты), [EXAMPLES.md](EXAMPLES.md#2--управление-контактами)

### Поиск и фильтрация

- **Поиск по имени** - нечеткий поиск по полю `name`
- **Поиск по email** - точный или частичный поиск по email
- **Фильтрация по владельцу** - фильтр по `owner_id`
- **Пагинация** - параметры `page` и `per_page` (по умолчанию 10 на страницу)

### Бизнес-правила

- **Защита от удаления**: нельзя удалить контакт, если у него есть активные сделки (возвращает 409 Conflict)
- **Валидация email**: автоматическая валидация формата email
- **Привязка к организации**: все контакты автоматически привязываются к организации из заголовка

---

## 💼 Управление сделками (Deals)

### Создание и управление

Полный набор операций для работы со сделками:

- **GET /api/v1/deals** - список сделок с фильтрацией и пагинацией
- **POST /api/v1/deals** - создание новой сделки
- **GET /api/v1/deals/{id}** - получение сделки по ID
- **PATCH /api/v1/deals/{id}** - обновление сделки
- **DELETE /api/v1/deals/{id}** - удаление сделки

**Подробнее:** [API.md](API.md#сделки), [EXAMPLES.md](EXAMPLES.md#3--управление-сделками)

### Воронка продаж (Pipeline Stages)

Настраиваемые стадии воронки продаж:

- **Lead** - Первичный контакт
- **Qualified** - Квалифицированная лида
- **Proposal** - Коммерческое предложение
- **Negotiation** - Переговоры
- **Won** - Выигранная сделка
- **Lost** - Проигранная сделка

Переходы между стадиями отслеживаются в таймлайне активности.

### Статусы сделок

- **new** - Новая сделка
- **in_progress** - В работе
- **won** - Выиграна
- **lost** - Проиграна

### Валюты

Поддержка множества валют:
- **USD** - Доллары США
- **EUR** - Евро
- **RUB** - Российские рубли

### Фильтрация и сортировка

- **По статусу** - фильтр по полю `status`
- **По стадии** - фильтр по полю `stage`
- **По сумме** - фильтр по диапазону `amount_min` и `amount_max`
- **По валюте** - фильтр по полю `currency`
- **Сортировка** - по полям `created_at`, `updated_at`, `amount`

### Бизнес-правила

1. **Запрет закрытия без суммы**: нельзя закрыть сделку как "выиграна" (won) с нулевой или отрицательной суммой
2. **Автоматическое логирование**: при изменении статуса автоматически создается запись в таймлайне
3. **Контроль переходов**: обычные пользователи не могут откатывать сделку на предыдущие стадии
4. **Привязка к контакту**: сделка должна быть привязана к существующему контакту

---

## ✅ Управление задачами

### CRUD операции

- **GET /api/v1/tasks** - список задач с фильтрацией
- **POST /api/v1/tasks** - создание новой задачи
- **GET /api/v1/tasks/{id}** - получение задачи по ID
- **PATCH /api/v1/tasks/{id}** - обновление задачи
- **DELETE /api/v1/tasks/{id}** - удаление задачи

**Подробнее:** [API.md](API.md#задачи), [EXAMPLES.md](EXAMPLES.md#4--управление-задачами)

### Привязка к сделкам

- Задачи всегда привязаны к конкретной сделке
- При создании задачи указывается `deal_id`
- Все задачи сделки доступны через эндпоинт `/api/v1/deals/{id}/tasks`

### Сроки выполнения (Due Date)

- Установка срока выполнения через поле `due_date`
- Валидация: дата выполнения не может быть в прошлом
- Фильтрация задач по дате выполнения
- Отслеживание просроченных задач

### Статус выполнения

- Поле `is_done` для отметки выполнения
- Фильтрация по статусу (выполненные/невыполненные)
- Автоматическое логирование при изменении статуса

### Контроль доступа

- **Owner/Admin/Manager**: могут создавать задачи для любых сделок
- **Member**: могут создавать задачи только для своих сделок (где пользователь является владельцем)

---

## 📝 Таймлайн активности

### История действий

Таймлайн отслеживает все действия по сделке:

- **GET /api/v1/deals/{id}/activities** - получение таймлайна сделки
- **POST /api/v1/deals/{id}/activities** - создание записи в таймлайне

**Подробнее:** [API.md](API.md#активности), [EXAMPLES.md](EXAMPLES.md#5--таймлайн-активности)

### Типы активностей

1. **comment** - Комментарии пользователей
   - Ручное создание комментариев
   - Хранение текста комментария

2. **status_changed** - Изменение статуса сделки
   - Автоматическое создание при изменении статуса
   - Сохранение старого и нового статуса

3. **task_created** - Создание задачи
   - Автоматическое логирование при создании задачи
   - Информация о созданной задаче

4. **system** - Системные события
   - Автоматические события системы
   - Логирование важных операций

### Хранение данных

- Поле `payload` (JSONB) для хранения произвольных данных
- Гибкая структура для разных типов активностей
- Сортировка по дате создания (от старых к новым)
- Информация об авторе каждой активности

---

## 📊 Аналитика и отчеты

### Сводка по сделкам

**Эндпоинт:** `GET /api/v1/analytics/deals/summary`

Статистика по сделкам организации:

- **Количество сделок по статусам** - разбивка по статусам (new, in_progress, won, lost)
- **Сумма сделок по статусам** - общая сумма в каждой категории
- **Средняя сумма выигранных сделок** - расчет среднего значения
- **Количество новых сделок за период** - фильтр по дате создания
- **Процент конверсии** - соотношение выигранных к общему количеству

**Подробнее:** [API.md](API.md#аналитика), [EXAMPLES.md](EXAMPLES.md#6--аналитика)

### Воронка продаж

**Эндпоинт:** `GET /api/v1/analytics/deals/funnel`

Анализ воронки продаж:

- **Количество сделок по стадиям** - распределение по стадиям воронки
- **Разбивка по статусам** - детализация внутри каждой стадии
- **Расчет конверсии** - процент перехода между стадиями
- **Визуализация воронки** - данные готовы для отображения в виде воронки

### Кеширование

- **In-memory кеширование** результатов аналитики
- **TTL (Time To Live)** для обновления кеша
- Оптимизация производительности при частых запросах
- Автоматическая инвалидация кеша при изменении данных

---

## 🏗️ Техническая архитектура

### Clean Architecture

Система построена по принципам Clean Architecture с четким разделением на слои:

1. **API Layer** (`app/api/`)
   - Роуты и эндпоинты
   - Валидация входных данных (Pydantic схемы)
   - Обработка HTTP запросов/ответов

2. **Service Layer** (`app/services/`)
   - Бизнес-логика
   - Правила валидации
   - Координация между репозиториями

3. **Repository Layer** (`app/repositories/`)
   - Доступ к данным
   - Абстракция над базой данных
   - Реализация паттерна Repository

4. **Model Layer** (`app/models/`)
   - ORM модели (SQLAlchemy)
   - Определение схемы базы данных
   - Связи между сущностями

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#-архитектура-проекта)

### Dependency Injection

- Использование FastAPI Depends для внедрения зависимостей
- Изолированное тестирование каждого слоя
- Легкая замена реализаций

### Асинхронная архитектура

- Все операции используют `async/await`
- Асинхронные запросы к базе данных
- Высокая производительность при большом количестве одновременных запросов

### Миграции базы данных

- **Alembic** для версионирования схемы БД
- Автоматическое применение миграций при старте
- Откат миграций при необходимости

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#миграции-базы-данных)

### Типобезопасность

- **Type hints** для всех функций и методов
- **Pydantic 2.x** для валидации данных
- **mypy** для статической проверки типов
- 100% покрытие типизацией

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#проверка-типов-mypy)

---

## 🧪 Качество кода и тестирование

### Unit-тесты

- Тестирование бизнес-логики изолированно
- Тестирование правил валидации
- Тестирование ролей и прав доступа
- Мокирование зависимостей

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#тестирование)

### Интеграционные тесты

- Тестирование API через TestClient
- Тестирование взаимодействия слоев
- Тестирование базы данных
- Полный цикл операций

### E2E тесты

- Полные сценарии использования (регистрация → аналитика)
- Реалистичные данные
- Проверка всей цепочки операций

### Статистика тестирования

- **186 тестов** покрывают всю функциональность
- Высокое покрытие кода
- Автоматический запуск тестов в CI/CD

### Линтеры и форматирование

- **black** - автоматическое форматирование кода
- **ruff** - быстрый линтер для Python
- **isort** - сортировка импортов
- **pre-commit** хуки для автоматической проверки

**Подробнее:** [DEVELOPMENT.md](DEVELOPMENT.md#качество-кода-и-линтеры)

### OpenAPI/Swagger документация

- Автоматическая генерация документации из кода
- Интерактивный Swagger UI на `/docs`
- ReDoc документация на `/redoc`
- Актуальная документация API

---

## 🐳 Docker-ready

Система полностью готова к развертыванию через Docker:

- **docker-compose.yml** для запуска всех сервисов
- **docker-compose.test.yml** для изолированного тестирования
- Автоматическое применение миграций при старте
- Готовые образы для production

**Подробнее:** [INSTALLATION.md](INSTALLATION.md)

---

**Назад к:** [README.md](../README.md)
